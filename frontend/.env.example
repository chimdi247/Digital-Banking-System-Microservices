# For local (non-Docker) development: `npm run dev` reads this via Vite.
# When running via the root docker-compose.yml, VITE_API_BASE_URL is passed
# in as a build arg instead — see ../.env.example at the repo root.
VITE_API_BASE_URL=http://localhost:8080
